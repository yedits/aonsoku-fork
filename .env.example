# Aonsoku Configuration

# Server Settings
PORT=8080

# Navidrome/Subsonic Server
SERVER_URL=http://localhost:4533
HIDE_SERVER=false

# Auto-login (use only in secure local environments)
APP_USER=
APP_PASSWORD=
APP_AUTH_TYPE=token

# Server Type (subsonic, navidrome, or lms)
SERVER_TYPE=navidrome

# UI Settings
HIDE_RADIOS_SECTION=false

# Upload Service (for music uploads feature)
VITE_UPLOAD_SERVICE_URL=http://localhost:3001
